-- ===================================================================
-- EdgeLink 菜单微调：「存量采集点监控」(3000) 提到 EdgeLink 一级目录，
-- 与「采集配置」(2046) 同级（原挂在「运行监控」2090 下）。
-- 幂等可重复执行；menu_id 不变，角色授权不受影响。
-- 日期：2026-08-30
-- ===================================================================

UPDATE sys_menu SET parent_id = 2083, order_num = 4 WHERE menu_id = 3000;

-- 回滚：
-- UPDATE sys_menu SET parent_id = 2090, order_num = 2 WHERE menu_id = 3000;
