# 2026-09-04 变更说明 — 存量监控登记优化 + 内存趋势

> 本目录只放一个 SQL：`migration_20260904_site_health.sql`（执行一次即可，幂等）。

## 一、变更清单

### 1. 登记页 IP 示例修正（register/index.vue）
- 办公网 IP 示例：`192.168.1.10` → `10.81.8.34`（办公网通常 10 段）；
- 工业网 IP 示例：`10.0.0.10` → `192.168.1.10`（工业网是 192.168 段）。

### 2. 采集场所拆分：栋别 / 楼层 / 工程（全部必填、自由文本）
- **表**：`site_health_site` 新增 `building` / `floor` / `process_stage` 三列（迁移见本目录 SQL）；
- **后端**：DO/VO（Register/Update/List 三个模型）/ Service（register_site、update_site）同步；
- **前端登记页**：新增三个必填输入；编辑对话框同样补齐；
- **监控页**：新增「位置（栋/楼/工程）」列（组合显示）。
- 存量数据三列为 NULL，编辑时按新必填规则补全。

### 3. 内存趋势图（监控页行内「趋势」按钮）
- **后端**：新增 `GET /site-health/site/{id}/trend?hours=168`——按小时分桶返回 `{bucket, avgMb, maxMb}`（MySQL date_format 聚合，7 天数据量从 2 万行压到 168 个点）；
- **前端**：监控页每行新增「趋势」按钮，弹窗内 ECharts 折线（实线=平均内存，虚线=峰值内存）；
- 空数据/未上报过的采集点显示空态提示。

## 二、改动文件

| 层 | 文件 |
|---|---|
| SQL | `docs/9-4/migration_20260904_site_health.sql`（唯一） |
| 后端 | `module_site_health/entity/do/site_health_do.py`、`entity/vo/site_health_vo.py`、`service/site_health_service.py`、`dao/site_health_dao.py`、`controller/site_health_controller.py` |
| 前端 | `src/views/plc/siteHealth/register/index.vue`、`src/views/plc/siteHealth/monitor/index.vue`、`src/api/plc/siteHealth.js` |

## 三、验证结果（已通过）

- 后端 py_compile 全过；前端两页模板/脚本解析全过；
- 迁移执行后三列就位；
- 趋势接口实测返回真实分桶数据（`2026-08-29 13:00 avgMb=89.5 ...`）。

## 四、部署注意

- 后端需重启（新接口 + 新列）；
- 前端重新打包；
- 存量采集点的 栋/楼/工程 为空，首次编辑时按新必填规则补全。
