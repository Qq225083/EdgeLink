# EdgeLink 10 天整改计划（目标：试运行水平）

> **外部评审修订（2026-08-02）**：独立评审（见《外部评审提示词.md》对应报告）后接受以下修订——
> 1. **Day 3 拆分**：Day 3 只做 P0-4 状态机（**episode 语义**+存量清理+通知补偿），P1-8/P1-18/P2-7 移到 Day 4。告警重开不用"90s 行龄阈值"（会造成已确认告警 90s 轰炸 → 确认疲劳），改为：恢复漏报视为新 episode 才重开+通知；告警行加 `last_notified_at`，持续故障重通知间隔 ≥30 分钟且降级为提醒。
> 2. **通知补偿**：翻转通知发送失败时置 `notification_pending`，下轮扫描重试（防 MQTT 抖动丢关键告警）。
> 3. **Modbus P0-1 已提前完成（本日）**：modbus-read emit `rawValue`=解码值、`rawWord` 保留首字；applyTransform/计算点位去 toFixed(4)；实测 FLOAT32=23.5/INT32=70000 通过，52 项测试全绿。另查明：modbus-write 为单点位写（FC05/06/16 单值多字），**无间隙填 0 风险**；modbus-pool.js 已抽共享池。
> 4. **MySQL 风险升级**：MySQL 绑 0.0.0.0:3308 且 root/*** 在 git 历史——**"MySQL 密码轮换或绑 127.0.0.1"提升为试运行前必做**（PG 被 pg_hba 限 localhost，可控）。
> 5. **EMQX 声明式备份**：Day 9 出配置导出 JSON + 恢复脚本（当前 EMQX 进程已停，导出待其启动后补做）。
> 6. **Day 8 去重策略**：`DISTINCT ON (node_id,device_id,tag_id,insert_time) ORDER BY ctid DESC` 保留每键最新一条，再建唯一索引。
> 7. **新增**：P1-NEW-1 配置快照（plc_publish_log 增 config_snapshot，回滚=重发旧快照）入 Day 4/9；准入检查单补 3 条（重通知间隔≥15min、断后端恢复无批量假告警、EMQX 配置 JSON 入库）；Day 10 前备 24h 自动采样脚本。
> 8. **节点 backlog**（不阻塞试运行）：轮次闸连续丢弃 N 轮升级可见性；写操作短排队再丢弃；Modbus 写节点 toFixed 核查（Day 5）。
> 9. resolve_alert 加 node_id=0 且 device_id=0 的防御（P2-NEW-2）。

> 问题编号引用《问题清单.md》。每日含：任务（按依赖排序）→ 验收（必须实测打勾）。
> 人力假设：1–2 名工程师；每天结束留 30 分钟站会同步与打勾。
> 总原则：**先止血（安全/监控说谎），再拆弹（数据类型语义），后加固（韧性），最后回归准入。**

## 整改前准备（Day 0，半天）

- [ ] 冻结变更：通知相关人员整改期间不在生产配置面做大改动；备份 MySQL `ruoyi` 库、PG `postgres` 库、`D:\nodered` 整目录。
- [ ] 建整改分支：`git checkout -b fix/2026-07-27-audit-round`；后端/前端/两个协议包/pg-store 各自梳理未提交改动（modbus v1.3.3、MC v1.4.3-dev 均在跑未提交代码）先提交入库。
- [ ] 搭验证环境：MC 模拟器（3E/4E 可切）、Modbus 模拟器、可随便停的 EMQX/PG 实例；准备 golden buffer 生成器（pymcprotocol 或本机 `node-red-contrib-mcprotocol` 包）。
- [ ] 确认 GitHub 仓库可见性（public/private），结果决定 P0-5 处置等级。

---

## Day 1｜安全止血：密钥与 EMQX

> **完成情况（2026-07-27 当日）**：
> - ✅ P1-11 完成：secret_key 改 SHA-256 哈希存储（存量 2 条已迁移）；列表接口不再下发 secretKey；新增/重置明文仅响应返回一次（前端弹密钥对话框）；顺带修复 /auto 500（DO 补 machine_fingerprint）、/auto 不再回吐已注册节点密钥、移除 fingerprint 劫持改写、消除 create 分支 MissingGreenlet 风险。实测：库存哈希、/bootstrap 明文校验通过（含迁移后存量节点）、/auto isNew=false 时 secretKey=None。
> - ✅ P0-5 大部分完成：JWT_SECRET_KEY/MONITOR_API_KEY/EDGE_COLLECTOR_PASSWORD/MQTT 账号 全部轮换并写入 4 个后端 env + 2 个前端 env（值不入文档）；edge_collector 账号 bcrypt 同步（登录实测通过）；8 个 .env 已 git rm --cached 并提交（commit 7387374），.gitignore 补 `.env.*` 规则（保留 .env.example）。**遗留**：① MySQL/PG 密码按决定暂不轮换（仓库若曾 public 仍需换）；② git 历史清洗（filter-repo+force-push）另约。
> - ✅ 实测：旧 JWT 401、旧 MONITOR_API_KEY 401（错误文案明确）、新心跳链（edge JWT+新 API Key）200。
> - ✅ P1-10 EMQX 完成（2026-07-27）：服务启动后全程经 18083 REST API 配置（未改任何配置文件）——启用内置数据库密码认证（sha256）；建三账号：`edgelink_backend`（仅 publish edgelink/notify/#）、`edgelink_edge`（订阅 notify/trigger + 发布 alarm/nodes/heartbeat）、`edgelink_frontend`（只读订阅 alarm/heartbeat/nodes）；授权源仅内置数据库（默认 file 源 acl.conf 的 `{allow,all}` 已删除）；`no_match=deny`；dashboard 默认口令 admin/public 已重置（新口令已当面交付机主）。实测（Node mqtt.js + broker 授权计数双重验证）：匿名/错密码拒连、backend 订阅被拒、edge 发布 notify 被拦截、edge 发布 alarm 正常、frontend 发布被拦截/订阅正常、后端 notifier 以 edgelink_backend 上线且通知发布成功。**遗留**：EMQX 重启后配置持久化于内置 mnesia（无需文件），但机主本次是以 `emqx.cmd start` 手工前台启动——生产建议改为服务方式并确认自启；后端<->边缘<->前端三账号 ACL 粒度已足够试运行，后续可把边缘账号再按节点细分。

**任务**
1. P0-5 密钥处置：轮换 JWT secret、MySQL/PG 密码、MONITOR_API_KEY、EDGE_COLLECTOR_PASSWORD、EMQX 凭据 → `git rm --cached` 8 个 .env → filter-repo/BFG 清历史 → .gitignore 补规则 → 清理 `docs/V9` 等历史文档硬编码凭据。
2. P1-10 EMQX 启用认证 + ACL（按 `docs/edgelink_mqtt_deploy.md`）：后端发布账号、边缘订阅账号、前端 WS 账号分离；`edgelink/notify/broadcast` 写权限仅后端。
3. P1-11 secret_key 改哈希存储（迁移现有 2 条记录）+ 列表接口排除该列。
4. 同步切换：后端 .env（环境注入）、Node-RED 启动环境、前端 `VUE_APP_MQTT_*`，避免互锁。

**验收**
- [ ] 仓库工作区与 git 历史均检索不到真实密钥；旧密钥全部失效。
- [ ] 匿名连 EMQX 被拒；用错误账号发 NODE_STATUS 被 ACL 拒。
- [ ] 密钥列表接口响应中无 secretKey 明文。

**风险**：轮换当天各组件切换顺序——先 EMQX 加账号（不断旧匿名），再切边缘/后端，最后关匿名。

---

## Day 2｜bootstrap 接入层重建（P0-2）

> **完成情况（2026-08-02 当日）✅ 全部实测通过**：
> - ✅ /auto 500 修复（Day 1 已做：DO 补 `machine_fingerprint`）；fingerprint 分支不再改写 host_pc_ip（劫持面关闭）；已注册节点不再回吐 secretKey（Day 1）
> - ✅ **按压配对注册窗口**：新增 `POST/GET/DELETE /plc/bootstrap-key/registration-window`（管理端开启，默认 10 分钟 TTL 自动关闭，存 Redis）；/auto 全新注册分支必须窗口开启，否则拒绝（Redis 故障 fail-closed）；已注册节点按 host_pc_ip/fingerprint 命中不受影响
> - ✅ **限流**：/auto 与 /bootstrap 各 20 次/分钟/IP（内存桶），实测 25 连发中 24 被限
> - ✅ 前端：bootstrap-key 页新增"开放注册"按钮 + 窗口状态标签（30s 轮询，beforeDestroy 清理）
> - ✅ 踩坑记录：@Log 切面要求被装饰函数带 DB session 参数（`log_annotation.py:67` 硬取 `session_name_list[0]`），纯 Redis 端点加 @Log 会 IndexError 500——registration-window 端点不加 @Log（用 logger 留痕）
> - ✅ 实测矩阵：窗口关→注册拒；开→注册得 secret（64hex 仅此一次）；/bootstrap 全量配置（edge MQTT 账号）；已注册再调 /auto→secretKey=None；关窗→再拒；停用→"节点已停用"文案（kill switch 重启路径完整）
> - 注：计划中的"secret 换取后作废"经实现期复核**调整为不作废**——边缘每次重启都要凭 secret 重新 /bootstrap（自愈设计依赖），作废会断重启链；实际防泄漏手段=哈希存储+窗口闸+限流+管理端 regenerate 轮换。

**任务**
1. DO 补 `machine_fingerprint` 列对齐库表；修 `/auto` 500（顺带处理 commit 后属性访问，P2-15）。
2. `/auto` 鉴权与发放策略：仅首次注册发放 secret；已注册节点须凭 secret 或管理端重置；fingerprint 变更需管理端确认，不允许接口直接改写 host_pc_ip。
3. `/bootstrap` 换取正式凭据后 secret 作废/轮转；加有效期与限流。
4. 回归新节点接入全流程（bootstrap→凭据→登录→拉点位→心跳）。

**验收**
- [ ] 无 token 访问两接口返 401/403；`/auto` 不再 500。
- [ ] 新节点首次注册→接入成功；同节点第二次经 `/auto` 领 secret 被拒。
- [ ] 换取正式凭据后旧 secret 失效。

---

## Day 3｜告警可信化 I：状态机与通知门控（按评审修订拆分后）

> **完成情况（2026-08-02）✅ 代码+实测证据**：
> - ✅ **通知门控（防风暴+防确认疲劳）**：`check_offline_nodes`/`check_offline_devices` 改为返回 notify_list，仅含三类：①本轮新建/重开（重开即新 episode，重置 last_notified_at）②`notification_pending=1` 待补偿 ③超 30 分钟未通知的持续故障（降级 severity=3 提醒）。不再"每轮全量通知"。
> - ✅ **通知补偿**：`publish_notification` 返回 bool；失败置 pending=1 下轮重试。实测：EMQX 停机下 3 条活动告警全部正确置 pending=1（DB 实证）。
> - ✅ **新增列**：`monitor_alert.last_notified_at` + `notification_pending`（ALTER 已执行 + DO 同步）。
> - ✅ **存量清理**：孤儿 open 告警 6 条 + device_id=0 脏行 1 条 + node_id=0 畸形行 1 条全部标记恢复；node 56 的 status=1 卡死告警保持确认态并纳入提醒链。
> - ✅ **P1-9 顺手修复**（Day 4 前移）：MQTT 启动失败不再"永不重连+单例不注册"——单例先注册（未连接 publish 返回 False 由 pending 补偿）+ 启动失败进指数退避链。
> - ✅ resolve_alert 0/0 防御（P2-NEW-2）。
> - ⏳ 待 EMQX 启动的活体验证：重连后 pending 自动冲刷 + last_notified_at 落库 + 持续故障 30 分钟才再次提醒（P1-9 修复后后端无需重启即自愈）。
> - 调整说明：原计划"status=1 行龄 90s 重开"按外部评审废弃（确认疲劳），改为 episode+提醒间隔模型。P1-18/P2-7 移到 Day 4。

**原任务（拆分前，已被上方修订覆盖）**
1. P0-4：`monitor_dao.create_alert` 命中 status=1 且行龄超阈值（建议 3× 心跳=90s）时重置 created_at/alert_msg 并重新通知；离线扫描对"确认但仍离线"周期再升级；清理 device_id=0 的 PLC_OFFLINE 产生路径。
2. 存量清理：解析/关闭孤儿告警（node_id 999/4975/59/60、device_id=0 行）；node 56 类卡死告警复盘重置。
3. P1-8：通知门控到"本轮新创建/状态翻转"（复用 DAO 的 existing_node_ids）；前端按 alert id 去重弹窗。
4. P1-18：心跳响应强制校正边缘 nodeId（不一致即更新）；`09` 删除哈希兜底撞号路径（失败即重试不再编造 id）。
5. P2-7 阈值统一：在线判定 60/75/90s 收敛为一个常量（建议 90s 三处共用）。

**验收**
- [x] 通知风暴消除：持续离线时每轮全量通知 → 仅新建/补偿/30分钟提醒（DB pending 状态实证）
- [x] 复现链路实证：EMQX 停机→pending=1 置位 → EMQX 恢复→后端 5s 级自动重连（P1-9，无需重启）→ pending 冲刷、last_notified_at 落库（20:50:30 实证）→ 后续扫描静默（风暴停止）
- [x] 持续离线节点提醒链：node 56 的 status=1 历史卡死告警已成功重通知（20:50:30）
- [x] 孤儿告警清零（6+2 条已清理）；EMQX 配置声明式备份完成（`docs/edgelink_emqx_config_backup.json`：authn×1/授权源×1/用户规则×3）

---

## Day 4｜发布链可信 + 前端批量修复（P0-3、P1-15、P1-9、P0-6）

> **完成情况（2026-08-02）✅ 实测证据**：
> - ✅ **P0-3 发布假成功根治**：`publish_config_refresh`/`publish_notification` 返回 bool；`publish_device_config` 逐设备收集成败；`plc_publish_log` 新增 `status`/`fail_detail`/`config_snapshot` 三列（迁移已执行）；接口如实返回（success/partial/failed + failedDevices 明细）；前端发布页部分失败弹警告框列设备名，履历页新增"结果"列（成功/部分成功/失败标签）+ 失败明细弹层。
> - ✅ **实测**：全量发布 → `status:success`，publish_log 新记录 `publish_by='admin'`（P1-15 修复，`CurrentUserDependency` 注入，告别 request.state.user 恒 None）、`device_ids='[30]'`（告别 'null'）、`config_snapshot` 2071 字节含设备+点位全字段。
> - ✅ **P1-NEW-1 配置快照**：快照随发布落库（回滚=重发旧快照的设备清单，完整"DB 级回滚"为后续迭代）。
> - ✅ **P1-9 已于 Day 3 提前完成**（启动失败进退避链 + 单例先注册），实证：EMQX 停机→恢复，后端 5s 级自愈未重启。
> - ✅ **P1-18 nodeId 幽灵根治**：边缘 `09` 删除哈希编造（失败即放弃本轮、30s 重试），调用点两处加守卫；心跳响应本就返回 node_id 且 `17:128-131` 已会校正——双链路闭环。权威 flow 与线上 `D:\nodered\flows.json` 同步更新。
> - ✅ **P2-7 阈值统一 90s**：后端 monitor_service 节点列表 60s→90s；前端 monitor 页 3 处 75s→90s。
> - ✅ **前端告警去重**：LWT 与后端通知双通道 60s 去重（`_shouldAlertPopup`），弹窗/响铃不再重复。
> - ⏭ 遗留：P0-6（batchUpdateTags 导出）与 P2-2（异常回显固定文案）未做，移 Day 9。

**原任务（已被上方覆盖）**
1. P0-3：`publish_notification` 返回真实成败；发布接口区分 notified/failed 并随响应返回；`plc_publish_log` 增 status/fail_detail 列（含迁移）；前端发布中心展示部分失败明细。
2. P1-15：`publish_by` 改用 `CurrentUserDependency()`；全量发布落实际 device_ids。
3. P1-9：MQTT start 失败进指数退避重连链，重连成功注册全局单例。
4. P0-6：`api/plc/tag.js` 补 `batchUpdateTags` 导出；顺手修 P2-29 前端确定按钮 loading 防重。
5. P2-2：全局异常回显改固定文案，SQL 细节只进日志。

**验收**
- [x] 发布如实：成功/部分/失败三态 + 明细；publish_log 记真实操作人与实际设备清单（实测通过；失败路径由 Day 3 pending 实证链覆盖）
- [x] publish_log 新记录含真实操作人；MQTT 断连恢复后通知链自愈（Day 3 实证）
- [ ] 批量修改点位功能端到端可用（P0-6 移 Day 9）

---

> **进度注记（2026-07-27 晚）**：机主要求提前完善 MC 节点包（`node-red-contrib-mitsubishi`），以下事项已**提前完成**（包版本 v1.4.4，mocha 39 passing，工作区待机主提交）：
> - P0-1（32/64 位旁路）**MC 侧已修**：emit `rawValue`=解码值、首字移 `rawWord`；**遗留**：Modbus 包同病未修（Day 5 必须同步，否则两侧契约不一致），07 下游天然兼容（只读 rawValue）。
> - P0-7（4E 帧）**已完成**：规范重排 + golden buffer 对拍通过。
> - P1-3（X/Y 进制）**已完成**：FX=8 进制/余=16 进制，config 补 series 字段。
> - P1-5（_closed 泄漏）**MC 侧已修**（6 处中止点统一 release）；**遗留**：Modbus 侧（Day 7）。
> - P2-16/17（slope=0、bitOffset 钳制）MC 侧已修；Modbus 侧与 07 遗留。
> - **新增**：UDP 传输（tcp/udp 可选）、per-PLC 轮次闸、catch 可捕获、编辑器三列补齐、测试套件重建（golden buffer+解码组合+UDP 端到端）。
> Day 5/6/7 相应缩减：Day 5 只做 Modbus 侧 P0-1+P1-1 死区；Day 6 只做 Modbus CR/DISCRETE+三层命名统一（4E/进制已完）；Day 7 只做 Modbus 侧 _closed。

---

## Day 5｜数据类型语义 I：32/64 位旁路与死区（P0-1、P1-1、P1-20、P2-16/19）

> **完成情况（2026-08-02）✅**：
> - ✅ **P0-1 两侧全闭合**：MC 侧（v1.4.4，提前完成）+ Modbus 侧（本日，emit `rawValue`=解码值/`rawWord` 首字、applyTransform 去 toFixed、计算点位同步）；实测 FLOAT32=23.5、INT32=70000 通过，Modbus 52 项测试全绿。
> - ✅ **P1-1 死区语义统一**：`plc_tag.report_deadband_ms` INT→FLOAT、默认 0、注释改"数值（工程单位）"（迁移已执行，存量 60 行 1000→0）；VO 两处改 float 默认 0；前端标签改"变化死区（数值）"+step 0.1+precision 2+默认 0；Excel 导入删除"死区（ms)"表头别名。**实测：API 写入 0.5 → DB 存 0.5 ✓**（期间发现并清理了 uvicorn 孤儿进程占用 9099 导致旧代码应答的问题——已单实例干净运行）。
> - ✅ **P1-20 质量码统一**：07 lastGood 过滤 `!==2`（永真 bug，坏值污染 lastGood）改 `==='GOOD'`；stale 标记数字 2 → 字符串 'UNCERTAIN'（OPC UA 语义全链一致）。
> - ✅ **P2-16 slope=0**：07 数据管道 + 09 映射 + 两协议包（此前已修）全部 `||` → null 感知。
> - ✅ **字序默认值陷阱（评审 E-P2-3）**：09 注入默认 `byteOrder:'BIG_ENDIAN'` → **`'LITTLE_ENDIAN'`**（MC 线序即小端，旧默认会交换 16 位字节）、`wordOrder` 默认 `'LOW_FIRST'`。⚠️ 行为变更：此前 byteOrder 为空的点位在旧默认下被字节交换——若现场真机数据此前"看起来正常"，需把这些点位显式设为 BIG_ENDIAN（当前部署为模拟器环境，无影响）。
> - ✅ **P1-4 CR/DISCRETE**：modbus ALIAS 补 CR/DISCRETE/CO/INPUT；后端 VALID_REGISTER_TYPES 补 DISCRETE；三层命名对齐。
> - 权威 flow 与线上 `D:\nodered\flows.json` 已同步（MD5 一致），tmp_extract/_current 已刷新。

**原任务（已被覆盖）**
1. P0-1：两协议包 emit 改为 `rawValue: entry.convertedValue`（保留 `rawWord` 备查），或 07 入口统一 `convertedValue ?? rawValue`；双侧同步 + 回归脚本。
2. P1-1：死区列 INT→FLOAT、默认 0；DO/VO/注释/前端文案统一"数值死区（工程单位）"；输入去 step=100 加 precision；**存量迁移**：现存 1000 → 0。
3. P1-20：质量码全链统一 0/1/2=GOOD/BAD/UNCERTAIN 并写接口文档；lastGood 统一字符串。
4. P2-16：slope=0 可表达（`??` 替代 `||`），三处（07、两协议包）同步；P2-19：FLOAT32/DOUBLE 截断策略一致并 README 声明。

**验收（里程碑：FLOAT 端到端打通）**
- [x] FLOAT32=23.5、INT32=70000 解码→emit 契约实测通过（协议层对拍）；DOUBLE/UINT32 在 MC 侧测试已覆盖（46 项）
- [x] 死区 FLOAT 写入实测（0.5 入库）；0.3/0.6 行为依赖边缘运行（待 Node-RED 起后复测）
- [ ] 既有 16 位点位数据无回归（待边缘上线后对比）

---

## Day 6｜数据类型语义 II：4E 帧与寻址（P0-7、P1-3、P1-4、P2-17/18/28）

> **完成情况（2026-08-02）✅**：
> - ✅ **P0-7 / P1-3 / P2-17(MC) / P2-18**：已于 MC 节点完善专项（v1.4.4）提前完成——4E golden buffer 对拍、X/Y 进制（FX=8/余=16）+config series、bitOffset 钳制、地址上限 0xFFFFFF，MC 套件 97 项全绿。
> - ✅ **P1-4 CR/DISCRETE**（Day 5 完成三层命名统一）：本日补**功能码回归测试**——mock PLC 抓包实证 CR→FC01、DISCRETE→FC02、HR→FC03（`modbus-read_spec.js` 新用例；踩坑记录：modbus 节点的配置引用字段是 `modbus` 不是 `plc`，测试 flow 写错字段导致首轮超时）。
> - ✅ **P2-17（Modbus 侧）**：`decodeValue` BOOL 取位加 0-15 钳制（越界归 0）。
> - ✅ **P2-28**：地址 >65535 拦截（用户此前已加，`modbus-read.js:145`）；编辑器补"位"列；字节序保留 AB/BA/ABCD 经典词汇（`normalizeByteOrder` 双词汇归一已兼容，新旧配置通吃）；modbus-write 无 toFixed 截断（核查完毕，backlog 关闭）。
> - ✅ **Modbus 套件 53 passing / 0 failing**。

**原任务（已被覆盖）**
1. P0-7：4E 帧按规范重排（请求 25 字节：序列号+固定值进副标题、数据长 0x0C；响应 endCode@13、dataStart=15、minLen=15；分帧偏移同步）；`test/mc-protocol_spec.js` 改 golden buffer 对拍。
2. P1-3：X/Y 进制规则改 FX→8 进制、其余→16 进制；`mitsubishi-config` 补 plcSeries 字段；单测 Q:X1A=26、FX:X17=15。
3. P1-4：modbus ALIAS 补 CR/DISCRETE（顺带 CO/INPUT）；前端/后端/节点三层命名统一（建议 DISCRETE→DI 或全链 DISCRETE，择一写进接口文档）。
4. P2-17：bitOffset 两包 clamp 0-15 + 越界 warn + q=2；P2-18：3 字节地址上限校验；P2-28：modbus 地址 >65535 拦截、编辑器 HTML 字节序下拉更新。

**验收**
- [x] 4E 请求/响应与 golden buffer 逐字节一致；3E 回归不破（MC 套件）
- [x] CR 点位抓包发 FC01、DISCRETE 发 FC02；Q 系 X/Y 单测通过（MC 套件）
- [x] modbus `npm test` 全绿（53 项，含 Day 5/6 新用例）

---

## Day 7｜边缘韧性：连接池、kill switch、缓存兜底、心跳口径（P1-5、P1-6、P1-7、P1-13）

> **完成情况（2026-08-03）✅**：
> - ✅ **P1-5**：MC 侧已于 v1.4.4 完成；Modbus 侧由机主 pool 重构（ownerId/nodeClosed/detachNode）完成并经外部评审工作区核实。共享池下沉未做（记录在下一轮）。
> - ✅ **P1-6 kill switch 兜底全链路**：① 心跳响应携带 `disabled` 标志（实测：密钥停用节点收到 `disabled:True`，无绑定节点 `False`）；边缘 17 每周期比对并执行停采/恢复（写 cache.disabled + 清配置 + 标记待刷新）；② `10` NODE_STATUS 空 host 不再放行（node_id 优先精确匹配，空 host 直接丢弃）；③ 后端禁建空 host 密钥（实测拒绝文案明确）；④ toggle 发布携带 node_id 定向（实测发布动作+代码链核实）。
> - ✅ **P1-7 #97 缓存兜底前移**：09 缓存加载块前移到 JWT 校验之前（实证：`edge_config_cache.json` 引用位置在 `if (!jwtToken)` 之前）——后端挂时重启边缘可按缓存采集。
> - ✅ **P1-13 计数口径**：pg-store 重试救回行补发 inserted 事件、3 处 dropped 事件带 failed 计数；16 计数更新把 replayed 计入 success、`edge_pg_queue` 死指标接线（buffered+retryBuffered）。
> - ⏭ P2-23/24/25（busy 计数/401 首跑解耦/09 锁悬挂）移 Day 9。
> - 注：验收项中"带载重部署 50 次"与"EMQX 停用→恢复停采"需 Node-RED 在线运行环境，留待边缘联调日实测。

**原任务（已被覆盖）**
1. P1-5：两包 `_closed` 中止点统一 releaseConnection/摘除队列；releaseConnection 幂等化；队列交接登记 users。**（如人力允许，本日启动池/解码下沉共享包，否则记入下一轮）**
2. P1-6：心跳响应携带 disabled 标志、边缘每周期比对；`10` 空 host 拦截；后端禁建空 host 密钥；NODE_STATUS 带 node_id 定向。
3. P1-7：`09` 缓存加载前移到 JWT 失败分支。
4. P1-13：pg-store 重试/replay 成功补发 inserted 事件；dropped 计入 fail；`edge_pg_writing/edge_pg_queue` 接线或删除。
5. P2-23/24/25：调度器 busy 丢弃计数；401 误判首跑解耦；09 锁悬挂与 attempt 污染修复。

**验收**
- [ ] 带载反复重部署 50 次无连接楔死、无 Queue full。
- [ ] 停 MQTT 执行停用→恢复后 ≤30s 停采；空 host 消息零误伤；重启后已停用节点不复活。
- [ ] 断后端重启 Node-RED → 边缘按缓存采集；恢复自动续上。
- [ ] 制造可恢复 PG 抖动后，心跳 success/fail 与 PG 实际行数对账误差 <1%。

---

## Day 8｜pg-store 加固与 PG 停机演练（P0-8、P1-12、P2-20/21）

> **完成情况（2026-08-03）✅ 演练实证**：
> - ✅ **存量体检+索引**：43649 行零重复（无需去重）；已建 `uq_plc_data_dedup(insert_time,node_id,device_id,tag_id)` 唯一索引（ON CONFLICT 去重从此真实生效）+ `idx_plc_data_tag_time(device_id,tag_id,insert_time DESC)` 查询索引。
> - ✅ **P1-12**：错误分类白名单（连接类才重试）+ close 落 spool，机主在 v1.6.0 已先完成，评审核实；本日补 stranded `.replaying` 双存在合并恢复（永久搁浅路径关闭）。
> - ✅ **P0-8**：replay 优先消费 `.1` 归档、完整重放后自动接力至清空；轮转覆盖非空旧 `.1` 前发 `spool_archive_overwrite` 事件 + 大字告警。
> - ✅ **P2-20**：executeInsert 分片插入（1000 行/片），65535 绑定上限不再贴线；分片重试配合唯一索引幂等。
> - ✅ **PG 停机演练（锁表 25s 注入故障，`test/pg_outage_drill.js`）**：300 行直写 + 故障期 200 行落 spool + 恢复后 100 行直写 → **600 行全部入库、重复键=0**（唯一索引对账）。pg-store 套件 8 项 + 演练 1 项全绿。
> - ✅ pg-store 版本收敛 v1.6.1（CHANGELOG + 头注释）。
> - ⚠️ 踩坑记录：演练数据 tag_id 首次用字符串触雷——`edgelink.plc_data.tag_id` 是 numeric 列（string 报 22P02→死信路径反而被顺带验证）。

**验收（里程碑：PG 停机演练）**
- [x] PG 写不可用 → 恢复：数据零丢失、零重复（唯一索引可查重验证）✅ 实测通过（锁表 25s 模拟）
- [x] 确定性错误批次直接落盘不再无限重试（字符串 tag_id 触发 22P02 → 死信，实证）；close 时内存缓冲落 spool（v1.6.0 已实现，代码核实）
- [ ] spool>100MB 轮转的 `.1` 消费（代码修复+评审核实；100MB 级活体演练留试运行观察期）

---

## Day 9｜后端契约与数据质量（P1-14、P1-16、P1-17、P1-19、P2 精选）

> **完成情况（2026-08-03）✅ 实测矩阵通过**：
> - ✅ **P1-14**：`PlcDeviceQueryModel`/`TagQueryModel` 覆写 `status=None`（查询默认全部状态），克隆/详情自动含停用点位。
> - ✅ **P1-16 点位查重**：新增/编辑均按 (device_id+tag_name)、(device_id+register_type+register_address) 应用层判重（实测：重名/同地址均拒绝且文案明确）。副作用注意：存量重名点位在改名前无法编辑（正是逼清理的设计意图）。
> - ✅ **P1-17**：编辑 dump 排除 `del_flag`（实测 delFlag=2 不落库）；新增 DAO 排除客户端传 id（实测 id=99999 注入被忽略，实际分配 62）。
> - ✅ **P1-19 地址校验**：`_validate_address_format` 按软元件进制（W/B+非FX的X/Y=16进制、FX的X/Y=8进制、余=10进制）——比驱动表 `^[0-9]+$` 精确（后者会误杀 16 进制地址）；实测 'abc' 被拒且文案明确。边缘 09 的 `parseInt||0` 改 NaN→-1（协议节点拦截）。
> - ✅ **P2 精选**：P2-1 分页 ge=1/le=500（实测 pageSize=0 变 422 而非 500）；P2-2 全局异常固定文案不再回显 SQL 细节；P2-4 心跳注册 savepoint（并发首报不再 500）；P2-5 心跳 ge=0+node_ip 长度限制；P2-12 发布跳过 driver_code=UNKNOWN 设备（记入 fail_detail）。
> - ✅ **EMQX 恢复脚本**：`tools/restore_emqx_config.py`（读 docs/edgelink_emqx_config_backup.json + .env 密码，幂等重放 authn/用户/ACL/settings，删 file 源）。
> - ⏭ P2-6（双重 PreAuth）、P2-23/24/25（边缘调度细节）、P1-19 前端即时校验、P2-22（15_握手标注）移下一轮。

**验收**
- [x] 重名/同地址点位被拒；PUT 带 del_flag=2 不改库；'abc' 保存被拒；新增自带 id 被忽略
- [x] `pageSize=0` 不再 500（422 参数校验）；心跳注册并发安全（savepoint）
- [ ] 克隆含停用点位设备后点位数一致（代码路径已修，留边缘联调日实测）
- [ ] 停用设备可经列表重新启用（P1-14 修复后前端"全部"可查停用项）

---

## Day 10｜全量回归 + 试运行准入

**任务**
1. 对照《问题清单.md》逐条验收打勾（P0×8、P1×20 必须有实测记录；P2 精选按完成度记录）。
2. 全链路 24h 稳定运行观察：采集成功率、PG 行数增长、告警行为、spool 积压、内存/句柄。
3. 清理：`tmp_extract` 过期副本、`D:\nodered\node-red-contrib-mitsubishi\` 孤立 package.json、flows.json 多余备份归档；重建后端 venv（P2 运维项）。
4. 文档：更新《EdgeLink系统详细设计说明书》中受影响的契约（分页 alias、质量码、死区单位、地址 0 基约定、驱动扩展流程）；本目录填写整改完成日期与责任人。
5. 按《试运行准入检查单.md》逐项签署；**全绿 → 进入试运行**；建议试运行观察期 2 周，重点盯：告警准确度、PG 对账、spool 积压、EMQX 互踢。

**验收**
- [ ] 准入检查单全绿签署；遗留项全部登记在案（编号+计划版本）。

---

## 依赖关系速查

- Day 5 的 FLOAT 端到端验收 ← 依赖 Day 0 模拟器就绪。
- Day 6 的 golden buffer ← 可用 pymcprotocol/本机 mcprotocol 包，不依赖真机。
- Day 8 的唯一索引 ← 必须先存量去重；PG 停机演练依赖 Day 5/6 的协议修复全部完成。
- Day 1 的密钥轮换 ← 需与 Day 2 的 bootstrap 改造协调（secret 哈希化在 Day 1，接口逻辑在 Day 2）。
- 共享包下沉（P2-27）为可选项，不阻塞试运行，但 `_closed` 修复必须两包同步（Day 7）。

## 本轮不做（明确出范围，防 scope 蔓延）

- 多实例/多节点主备切换的深度演练（`docs/edgelink_multipc_deployment.md` 对应能力）。
- triggerKind=0 握手触发的完整实现（当前标注未实现即可）。
- pg_store TimescaleDB hypertable 启用评估（`useTimescaleDB=false` 现状保持）。
- 协议包共享库重构（如 Day 7 人力不足）。

---

## 补遗：第四轮从头独立审查（2026-08-19）发现与修复

> 外部 AI 按《外部评审提示词-从头独立审查.md》从零审查（不读既往结论），全部结论经我逐条实证复核。**无虚报、无误报，质量极高。**

### 已修复（全部带实证/回归测试）

| # | 问题 | 修复 |
|---|---|---|
| P0-1 | **调度器扇出丢失**（单输出 function 返回 flat 数组只发首设备，多设备饿死；实测 Node-RED 语义确认） | `return [out]` 嵌套数组逐条扇出；实证 `[1,2,3]` 全部送达 |
| P0-2 | **编辑设备静默重置非平铺协议参数**（unitId/rack/slot/serialPort 被 default 覆盖） | `applyDriverSchemaDefaults` 按 flatFields 区分取值源（form vs form.protocolParams） |
| #5 | BOOL 解码忽略 byteOrder（MC+Modbus 两处，BE 下位偏移错 8 位——**我 v1.4.4 引入**） | 两包 BOOL 分支先换序再取位 + 回归测试（0x0100 BE→bit0=1） |
| #6 | ON CONFLICT 目标与实表唯一索引不匹配（**我 Day-8 建 4 列索引与代码 3 列目标漂移**） | MC 格式 3 列（其自动建表自洽）+ batch 格式 4 列（对实表），缺列走 degrade；spec 加 batch 4 列用例 |
| #9 | 分片中途失败整批重灌重复行（**我 v1.6.1 分片引入**） | 各回灌/落盘点只保留未成功分片（slice(result.count)） |
| #3 | 多 BOOL 同寄存器不同位被去重折叠 | dedup key 含 dataType+bitOffset |
| #4 | Modbus BOOL 位写 FC06 整字覆写清其余 15 位 | 字寄存器 BOOL+bitOffset 走 RMW（FC03→改位→FC06） |
| #2 | 07 失败路径 MQTT 消息数组包裹丢 topic | 嵌套数组逐条扇出 |
| #1 | MC 连接池旧 socket 迟到 close 误杀新条目 | close/end/error 回调校验 socket 身份（read+write 双侧；Modbus 池重构本就有） |
| #13 | 脚本硬编码凭据 | observe_trial.py/drill 去默认值；7 个迁移脚本改 env 读取 |

### 接受为后续项
- #8 spool 覆盖：残余风险保留（PG 长停机>200MB），已加覆盖告警；建议轮转保留份数可配（下轮）
- #10 时序表 retention（TimescaleDB drop policy）——下轮
- #14 secret 走 header 传（弃 URL query）——下轮
- #15 全节点共享凭据文档化——下轮
- #17/#18 前端双上下文/schema 加载——下轮
- #20 后端 pytest 化 + drill 进 CI——下轮
- P2 杂项（plc_data 三套表漂移、实验表清理等）——登记

### 教训（写入团队知识）
1. **每次 merge 前必须全量回归**——本次回归当场抓到 2 个失败（其中 1 个是我自己 v1.6.1 引入的 ON CONFLICT 漂移、1 个是 ASCII 新代码的越界崩溃）。
2. **评审生产环境改动时标注"是否改者引入"**——本轮 3 个确认问题是我自己在整改中引入的（#5/#6/#9），交叉评审的价值就在这。
3. **测试前提审查**：两处失败测试是"前提写错"而非代码错（asciify vs toAsciiHex 是两种约定不能互拍；期望值抄错字节序）。
