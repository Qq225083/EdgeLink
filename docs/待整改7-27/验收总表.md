# EdgeLink 整改验收总表（Day 10 · 2026-08-03）

> 逐条对应《问题清单.md》。状态：✅已修复并实测 / 🔧已修复待边缘联调实测 / ⏭移交后续迭代 / ❌未修（含理由）。
> 测试基线：MC 套件 121 项全绿（含万点压测）｜Modbus 套件 55 项全绿（含 FC01/02 抓包回归）｜pg-store 套件 8 项 + PG 停机演练（600 行零丢失零重复）｜后端全接口冒烟通过。

## P0 逐项终态

| # | 问题 | 状态 | 证据/说明 |
|---|---|---|---|
| P0-1 | 32/64 位点位数据被死字段旁路 | ✅ | MC（v1.4.4）+Modbus（rawValue=decoded/rawWord 首字）双侧修复；实测 FLOAT32=23.5、INT32=70000；UDP e2e 断言 |
| P0-2 | bootstrap /auto 500 + 双接口零鉴权 | ✅ | DO 补 machine_fingerprint（500 修复）；**按压配对注册窗口**（Redis TTL 10min，fail-closed）+限流 20/min/IP；实测矩阵 9 项全过 |
| P0-3 | 配置发布假成功 | ✅ | publish 返回真实成败三态+明细；publish_log 增 status/fail_detail/config_snapshot；实测 publish_by='admin'、快照 2071 字节落库 |
| P0-4 | 告警"已确认卡死" | ✅ | episode 语义（重开=新 episode 重置通知态）+30min 提醒间隔+pending 补偿；实证：EMQX 停机 pending=1 → 恢复自动重连冲刷 → node56 卡死告警治愈 |
| P0-5 | 密钥随 git 入库 | ✅（附遗留） | 8 个 .env 退出 git（commit 7387374）+全密钥轮换+实测旧密钥失效；遗留：git 历史清洗另约、MySQL 密码仍待轮换（见"待机主事项"） |
| P0-6 | 前端批量修改必炸 | ✅ | api/plc/tag.js 补 batchUpdateTags；实测 PUT /plc/tag/batch 成功 |
| P0-7 | 4E 帧布局不符规范 | ✅ | 按 SLMP 重排+golden buffer 对拍（pymcprotocol/SH-080008 三方互证）；MC 套件内 |
| P0-8 | spool .1 归档静默销毁 | ✅ | 归档优先重放+接力清空+覆盖前告警事件；PG 停机演练 600 行零丢失零重复 |

## P1 逐项终态

| # | 问题 | 状态 | 说明 |
|---|---|---|---|
| P1-1 | 死区语义三层漂移+默认 1000 | ✅ | INT→FLOAT、默认 0、三层文案统一、存量 60 行重置、API 写 0.5 实测入库；迁移脚本 `docs/migration_20260802_day5_deadband.sql` |
| P1-2 | driverCode 可插拔名不副实 | ⏭ | 框架性改造（查询列+分流+下发三处断链），移交下一轮 |
| P1-3 | MC X/Y 进制（Q/L 错按 8 进制） | ✅ | FX=8/余=16 + config series 字段 + 单测 |
| P1-4 | Modbus CR/DISCRETE 降级 FC03 | ✅ | ALIAS 补全+三层命名统一+mock 抓包 FC01/FC02 回归 |
| P1-5 | 两包 _closed 中止泄漏 | ✅ | MC v1.4.4 六处统一释放；Modbus 侧机主 pool 重构（ownerId/nodeClosed）经评审核实 |
| P1-6 | kill switch 漏接无兜底+空 host 误伤 | ✅ | 心跳带 disabled（实测 True/False）+17 周期比对+10 空 host 拦截+禁空 host+node_id 定向；四通道闭环（MQTT/心跳轮询/重启 bootstrap/重启缓存） |
| P1-7 | #97 离线缓存兜底不可达 | ✅ | 09 缓存加载前移至 JWT 校验前（实证顺序） |
| P1-8 | 告警通知风暴 | ✅ | 门控（新建/补偿/30min 提醒）；实证：冲刷后 8 分钟扫描静默 |
| P1-9 | MQTT 启动失败永不重连 | ✅ | 单例先注册+启动失败进退避链；实证 EMQX 恢复 5s 级自愈（后端未重启） |
| P1-10 | EMQX 匿名+可伪造 kill switch | ✅ | 内置 DB 认证+三账号 ACL+no_match=deny；实测矩阵全过（匿名拒/伪造 notify 拦截/投递正常） |
| P1-11 | secret_key 明文+回显 | ✅ | SHA-256 哈希存储+列表剔除+明文仅一次返回 |
| P1-12 | pg-store 四连（去重失效/重放重复/搁浅/无限重试） | ✅ | 唯一索引兜底去重+错误白名单+搁浅合并+close 落盘（v1.6.0/v1.6.1） |
| P1-13 | 心跳健康指标失真 | ✅ | 重试/replay 计 success、dropped 计 fail、edge_pg_queue 接线；剩余：edge_pg_writing 恒 false 待接线（P2） |
| P1-14 | status 默认 '0' 静默过滤 | ✅ | Query 模型覆写 None；克隆/详情含停用项 |
| P1-15 | publish_by 永远 NULL | ✅ | CurrentUserDependency；实测 'admin' 落库 |
| P1-16 | 点位无查重 | ✅ | 新增/编辑双查重，实测拒绝文案明确 |
| P1-17 | 编辑可写 del_flag/id | ✅ | dump 排除；实测 delFlag=2 不落库、id=99999 注入被忽略 |
| P1-18 | node_id 幽灵数据 | ✅ | 边缘删哈希编造（失败即重试）；心跳响应本就带 node_id 且 17 会校正——双链路闭环 |
| P1-19 | 寄存器地址全链零校验 | ✅ | 后端按软元件进制校验（比驱动表 ^[0-9]+$ 精确，误杀 16 进制问题已规避）；09 NaN→-1 |
| P1-20 | 质量码语义半错位 | ✅ | 07 过滤 bug 修复（!==2 永真）+stale 统一 'UNCERTAIN' 字符串 |

## P2 处置摘要

- **已修**：分页界（ge=1/le=500）、异常回显固定文案、心跳注册 savepoint、心跳校验 ge=0、UNKNOWN 驱动不发布、slope=0 全链、FLOAT 精度截断、阈值统一 90s、前端告警去重、bitOffset 双包钳制、Modbus 编辑器位列、DDL 三方漂移（以库为准）、孤立包/备份归档、Excel"死区(ms)"表头移除
- **移交后续**：共享驱动包重构、P2-23/24/25 边缘调度细节、P2-6 双重 PreAuth、P2-13 文档对齐、MC 包 ASCII/软元件扩展/STRING（用户 v1.7.x 已在推进 ASCII）、轮次闸连续丢弃可见性、写操作短排队
- **设计声明**（写入文档）：07 工程值 toFixed(2) 为当前语义（全量数据已按此存储，变更需单独评估）；MC 写操作非 ACID 事务（MC 协议限制）

## 试运行准入检查单状态

- **1 安全准入**：✅ 全部通过（密钥出库+轮换、bootstrap 窗口闸、EMQX 认证 ACL、secret 哈希、编辑越权、异常不泄露 SQL）——除 **MySQL 密码仍待轮换/绑 localhost**（见待机主事项）
- **2 数据正确性**：✅ 协议层全绿（对拍/往返/压测）；FLOAT 端到端全链（边缘在线）待联调日最终确认
- **3 监控告警**：✅ 状态机/门控/补偿/自愈全实证
- **4 韧性**：✅ PG 停机演练通过；🔧 带载重部署 50 次、kill switch 全链、断后端缓存采集——需 Node-RED 在线日实测
- **5 性能资源**：🔧 24h 稳定运行项需边缘环境（见 tools/observe_trial.py 采样脚本）
- **6 运维文档**：✅ EMQX 备份 JSON+恢复脚本；🔧 flows 未推送远端、npm 未发布（机主动作）

## 结论

**当前状态已达"有条件试运行"门槛**：安全准入（除 MySQL 密码）、数据正确性（协议层）、监控告警、PG 韧性四大区块全部有实测证据。剩余全部为"需边缘环境在线"的联调项与机主动作项，见《总结与待机主事项》。
